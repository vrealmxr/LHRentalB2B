<?xml version="1.0" encoding="utf-8"?>
<!-- The "LH" mark cropped from the live site's own logo.png, centered in the
     adaptive icon's safe zone. Source is a small raster (the site never
     shipped a vector/high-res logo) — revisit with a proper vector mark if
     one becomes available. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:width="60dp"
        android:height="60dp"
        android:gravity="center">
        <bitmap
            android:src="@drawable/lh_monogram"
            android:gravity="center" />
    </item>
</layer-list>
